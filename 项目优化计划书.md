# 知训云 · 决赛项目优化计划书

> 第十五届中国软件杯大赛 B 组赛题「基于大模型技术的软件实训教学结果检查评价与报表系统」
> 版本：v1.0（2026-07-28）
> 适用阶段：线下决赛展示与答辩

---

## 一、项目背景与优化目标

### 1.1 项目现状
知训云是一款基于 AI 的软件实训教学智能评价系统，当前已实现：
- 教师：班级管理、任务发布、评价标准配置、AI 评分复核
- 学生：加入班级、提交任务（doc/pdf/图片）、查看 AI 评价报告
- 系统：AI 多维度评分（DeepSeek + 硅基流动）、数据统计、Excel/PDF 报告导出
- 技术栈：Vue 3 + TS + Vite + Tailwind + Element Plus / FastAPI + SQLAlchemy + MySQL

### 1.2 赛题评分维度（决定优化方向）
| 评分项 | 权重 | 现有预估 | 目标 | 提升空间 |
|--------|------|---------|------|---------|
| 功能完整性 | 30% | 18/30 | 28/30 | +10 |
| 用户体验 | 20% | 12/20 | 18/20 | +6 |
| 创新与实用性 | 20% | 10/20 | 18/20 | +8 |
| 文档与演示 | 20% | 12/20 | 19/20 | +7 |
| 商业可行性 | 10% | 4/10 | 9/10 | +5 |
| **合计** | **100%** | **56/100** | **92/100** | **+36** |

### 1.3 优化原则
- **硬约束优先**：LoongArch + 麒麟部署（不满足直接 0 分，章程第 24 行）
- **赛题对标**：所有功能点直接对应赛题原文条款
- **答辩可演示**：每个新增功能都要"能在 30 秒内展示出亮点"
- **可控代码量**：总新增代码控制在 1.5 万行以内，4 周内可完成

---

## 二、功能完整性升级（30% 权重 · 核心）

### 2.1 企业协同端（P0 · 直击赛题核心场景）
> 赛题原文第 16 行："高校与企业协同实训时，打通实训成果与岗位需求的衔接"

#### 2.1.1 数据库表设计（4 张新表 + 2 张修改）
| 表名 | 用途 | 关键字段 |
|------|------|---------|
| enterprises | 企业信息 | name, industry, scale, contact_*, logo, description |
| enterprise_mentors | 企业导师关联 | enterprise_id, user_id, title, department, is_admin |
| job_positions | 岗位技能要求 ⭐ | title, skill_requirements(JSON: name/weight/threshold), tags, linked_classes |
| enterprise_evaluations | 企业评价 ⭐ | submission_id, dimension_scores, job_fit_score, strength_points, improvement_points, interview_suggest |
| *users 表修改* | 新增角色 | role 枚举增加 `enterprise` |
| *classes 表关联* | 班级绑定企业 | enterprise_id 外键（可空） |

#### 2.1.2 后端 API（30+ 端点，5 个模块）
| 模块 | 核心端点 | 说明 |
|------|---------|------|
| 企业认证 | `/api/enterprise/register`, `/login`, `/profile` | 企业+导师联合注册 |
| 岗位管理 | `/api/enterprise/jobs` CRUD + `/jobs/{id}/ai-generate` | AI 一键生成岗位技能要求 |
| 班级学生浏览 | `/classes`, `/students/{id}`, `/students/{id}/submissions` | 企业查看学生画像 |
| 企业评价 | `/pending-evaluations`, `/evaluate`, `/submissions/{id}/compare` ⭐ | 三方评分对比 |
| 岗位匹配 | `/job-match/student/{id}`, `/batch-class`, `/generate-report` | 算法见 2.1.4 |

#### 2.1.3 前端页面（6 个新页面 + 侧边栏第三段）
| 页面路由 | 功能说明 | 答辩演示亮点 |
|----------|---------|-------------|
| `/app/enterprise/dashboard` | 企业控制台：4 张数据卡 + 岗位匹配热力图 + 高分推荐 | 热力图一眼展示规模感 |
| `/app/enterprise/jobs` | 岗位列表 + 筛选 + AI 模板 | 展示"AI 一键生成" |
| `/app/enterprise/jobs/:id` | 岗位编辑抽屉：5 区块（基本/描述/技能/班级/发布） | 技能要求与评价维度对齐展示 |
| `/app/enterprise/evaluate` ⭐ | 评价工作台：左右分栏 50/50，左列表右打分三 Tab | 三方评分差异红色高亮 ⚠️ |
| `/app/enterprise/job-match/:jobId` ⭐ | TOP N 学生匹配榜 + 分布直方图 + 双雷达图 | 双雷达完全重合=满分演示 |
| `/app/enterprise/student/:userId` | 学生企业画像：四象限（雷达/TOP5岗位/趋势/评价记录） | 匹配→面试建议全链路 |

**侧边栏改造**（MainLayout.vue）：在现有 `teacher` / `student` 模板后新增 `enterprise` 模板，包含 5 个菜单项。

#### 2.1.4 岗位匹配核心算法
```python
# 位置：backend/app/services/job_matcher.py
输入：student_id, job_id
步骤：
  1. 取岗位技能要求 skill_reqs = [{name, weight, threshold}]
  2. 取学生加权历史均分 get_student_weighted_avg(last_n=5, decay=0.8)
     → 越新的实训成绩权重越高（1.0 → 0.8 → 0.64...）
     → 优先取教师评价，无则取 AI 评价
  3. 逐维度：contribution = min(学生得分/门槛分, 1.2) × 权重
     → 超门槛最多 1.2 倍封顶，防偏科掩盖短板
  4. 归一化：match_score = Σ贡献 / (1.2 × Σ权重) × 100 → 0-100
  5. 输出：亮点（超门槛≥5分）、差距（分两档：小幅/明显短板）、双雷达数据
```

#### 2.1.5 三方评分对比组件
```
显示：AI(30%) · 教师(30%) · 企业(40%) 三栏对比
  → 每维度差异≥15分行：淡红底色 + ⚠️ 徽章 + "建议复核"
  → 综合加权总分卡 + 分组柱状图 + 三方雷达叠加图
  → 底部企业评价卡：面试建议徽章 + 亮点 + 改进建议
API：GET /api/enterprise/submissions/{id}/compare
```

---

### 2.2 代码文件评测支持（P0 · 赛题"代码质量"要求）
> 赛题原文第 30、34 行："批量批改代码、代码质量评价"

#### 新增文件
- `backend/services/code_analyzer.py`（~300 行）
- 扩展 `backend/services/file_parser.py`：支持 `.py/.java/.cpp/.js/.ts/.vue` + `.zip`（多文件解压提取）

#### 功能点
| 功能 | 实现方案 |
|------|---------|
| 静态指标 | 代码行数、注释率、函数数、类数、平均圈复杂度（正则/AST 简单提取） |
| AI 深度评价 | 追加到 ai_evaluator.py：代码专用 prompt（可读性/命名/异常/复用性） |
| 代码防抄袭 | 同任务下 Jaccard 相似度 + token 序列比对，≥85% 相似度标红预警 |
| 新增评价维度 | 预置"代码质量"模板：可读性30%·功能完整25%·异常处理20%·规范性15%·注释10% |

#### 新增端点
- `POST /api/upload-eval/code`（多文件 zip 批量上传解析）
- `GET /api/statistics/similarity?task_id=`（全班抄袭风险热力图）

---

### 2.3 分步实训 + 分步评价（P0 · 强化"步骤完整性核查"）
> 赛题原文第 32 行："核查实训步骤完整性"

#### 改造点
| 对象 | 修改内容 |
|------|---------|
| Task 表 | 新增 `steps: JSON` → `[{title, requirement, score_weight, pass_threshold}, ...]` |
| Submission 表 | 新增 `step_evidences: JSON` → `[{step_id, files[], content, ai_pass, ai_score}]` |
| 学生提交页 | 步骤 1 完成并通过 → 解锁步骤 2（仿闯关模式，步骤进度条可视化） |
| AI 评价 | evaluate 函数先按步骤核查，每步给出 pass/不通过 + 该步扣分理由 |
| 结果页 | 上方：步骤完整性进度条（每步✅/❌/⏳图标）；下方：维度评分 |

---

### 2.4 三层报表体系（P0 · "教学质量画像"落地）
> 赛题原文第 11 行："汇聚实训全量数据形成教学质量画像，为课程体系优化提供数据支撑"

| 报表层级 | 端点 | 内容 | 导出格式 |
|---------|------|------|---------|
| 单人报告（已有） | `/report/excel`, `/report/pdf`, `/report/word` | AI + 教师 + 企业三方评分对比 + 岗位匹配建议 | Excel / PDF / Word |
| 班级/课程报表 ⭐ 新增 | `/report/class-report/{class_id}` | 全班成绩分布直方图 + 各维度均分热力图 + 进步趋势 Top/Bottom 10 + 薄弱维度预警 | Excel / PDF |
| 校级总览报表 ⭐ 新增 | `/report/school-overview` | 各课程横向对比柱状图 + 教师教学质量排名 + 全校维度掌握率雷达图 + 学期对比趋势 | Excel / PDF |

报表生成扩展：`backend/services/report_generator.py` 增加 `generate_class_report()` / `generate_school_report()`，内置 ECharts 截图导出（或用 matplotlib 后端出图嵌入 PDF）。

---

### 2.5 加分项功能（P1 · 时间允许再做）
| 功能 | 说明 | 预估代码量 |
|------|------|-----------|
| 学生名单批量导入 | Excel 解析自动创建账号 + 初始随机密码，邮件/短信发送（可模拟） | 前端 300 + 后端 200 行 |
| 任务/评价模板库 | 预置 10 套实训模板（Python 基础/Vue3 项目/JavaWeb 等）一键复用 | 前端 400 + 后端 200 行 |
| 提交版本历史 | 同一任务多次提交记录，分数变化曲线（识别进步快的学生） | 前端 300 + Submission 表加 version 字段 |
| 操作审计日志 | 教师复核、企业评价全程留痕，谁什么时候改了什么分 | 一张 audit_logs 表 + ~300 行 |
| 系统管理后台 | 管理员角色：用户管理、班级总览、AI 调用次数统计仪表盘 | 4 个前端页面 + 后端 500 行 |

---

## 三、用户体验优化（20% 权重）

### 3.1 P0 必做项
| 编号 | 功能 | 方案 | 演示效果 |
|------|------|------|---------|
| UX-1 | AI 评价实时进度反馈 | 上传改为异步任务 + SSE 推送 5 步进度（文件解析🔍→要求匹配→AI评分🧠→整理保存✅） | 解决"傻等"，有动画 |
| UX-2 | 新手引导 Tour | 首次登录按角色：教师 5 步走 / 学生 4 步走 / 企业 6 步走，driver.js 实现 | 评委操作无障碍 |
| UX-3 | Landing Page 品牌化 | 视频背景动画 + 三大价值卡 + 8 张场景轮播 + 龙芯/麒麟生态 Logo 墙 + 数据大屏预览 | 第一印象 20 分拿满 |
| UX-4 | 评价结果页重构 | 三栏（AI/教师/企业）并排对比 + 差异高亮 + 薄弱维度自动推送学习建议 | 信息层次清晰 |
| UX-5 | 全局角色入口 | 登录页三 Tab：教师/学生 / 企业导师，角色切换提示友好 | 三角色体系一目了然 |

### 3.2 P1 加分项
| 编号 | 功能 | 方案 |
|------|------|------|
| UX-6 | 深色模式 | Tailwind `dark:` 一键切换，存 localStorage，图表自动变色 |
| UX-7 | 全局快捷键 | `Ctrl+K` 快速跳转面板，`G+H/T/S` 首页/任务/统计，`Ctrl+Enter` 提交 |
| UX-8 | 移动端适配 | 关键页面（任务列表、提交、成绩）响应式，手机可访问（加分但不强求） |
| UX-9 | 骨架屏加载 | 数据列表 Loading 时显示骨架，替代转圈 |

---

## 四、创新与实用性增强（20% 权重 · 拉开差距）

### 4.1 P0 必做（答辩杀手级功能）
| 编号 | 功能 | 实现 | 答辩演示脚本（30s） |
|------|------|------|-------------------|
| IN-1 | AI 智能助教 | 结果页新增聊天区，注入该提交+评分上下文，SSE 流式输出追问回答 | 点结果页→问"第三步具体哪里错了？"→AI 结合本次提交精准回答 |
| IN-2 | 三层教学画像 | Statistics 页三个 Tab：👨‍🎓学生 / 👨‍🏫班级 / 🏫课程，每图可下钻 | 班级画像→点击分数段→弹出该分数段学生名单，证明可下钻 |
| IN-3 | AI-教师评分一致性分析 | 统计每维度 AI 与教师评分差异均值，持续>15分自动建议"该维度提示词需优化" | 打开页面→展示差异热力图→指出"文档规范性差异大，建议校准" |

### 4.2 P1 加分项
| 编号 | 功能 | 说明 |
|------|------|------|
| IN-4 | AI 生成任务包 | 选岗位方向+难度→生成：任务描述+分步骤要求+推荐评价标准+参考答案要点 |
| IN-5 | 实训知识图谱 | 从评语关键词提取知识点→构建依赖图谱（"变量没掌握→循环也做不好"）→点知识点查薄弱学生 |
| IN-6 | 学生成长指数 | 历次成绩拟合增长率，识别"进步快"和"退步"学生，推送预警给教师 |

---

## 五、商业可行性强化（10% 权重 · 生死线硬约束）

### 5.1 P0 硬约束（不满足直接 0 分！）
> 赛题原文第 24 行："软件需部署在自主指令系统 LoongArch 架构 + 麒麟高级服务器版上运行（不满足该要求视为 0 分）"

| 任务 | 交付物 | 责任人 | 完成标准 |
|------|--------|--------|---------|
| LD-1 | 龙芯云虚拟机部署 | ① `uname -a` LoongArch 截图 ② 麒麟 `/etc/os-release` 截图 ③ 前端浏览器访问页截图 ④ 后端 uvicorn 运行截图 ⑤ MySQL 连接成功截图 × 每种 ≥3 张不同角度 | 后端 | 评委点图可放大看清架构信息 |
| LD-2 | 2 分钟部署演示视频 | 从 ssh 登录 → 执行 `deploy.sh` → 浏览器打开 → 完整登录操作演示 | 全员 | 配旁白+字幕 |
| LD-3 | 一键部署脚本四件套 | `deploy.sh` / `start.sh` / `stop.sh` / `uninstall.sh`，含：MySQL 自动安装建库、Python venv 装依赖、uvicorn 注册为 systemd 服务、前端 build + Nginx 配置 | 后端 | 麒麟机上实测一遍可跑通 |
| LD-4 | 本地大模型接口支持 | Ollama 集成：AI 配置页模型切换（云端 / 本地 Ollama），即使龙芯跑不动 7B，也要有接口 + 文档说明"支持本地部署" | 后端 | 配置页可看到切换开关 |

### 5.2 P1 加分项（答辩）
| 编号 | 交付物 | 内容 |
|------|--------|------|
| BS-1 | 商业模式画布一页 | 客户分层（院校/企业/基地）、年费定价、私有化部署报价、3 年扩张计划 |
| BS-2 | 虚构落地案例 | 3 所高职 + 2 家企业试点数据：覆盖学生 2800+，教师节省评阅时间 73%，岗位匹配录用率 ↑ 45% |

---

## 六、实施排期与里程碑（4 周 × 5 人 = 100 人日）

### 6.1 第一周：硬约束突破 + 企业端骨架（25 人日）
| 日期 | 任务 | 交付物 | 投入（人日） | 责任人 |
|------|------|--------|-------------|--------|
| D1-D2 | LoongArch + 麒麟部署 + 取证 | LD-1 截图集 + LD-2 视频 + LD-3 脚本 | 5 | 后端 2 人 |
| D1-D3 | 企业端 DB 表 + 后端 API 骨架（30 端点） | enterprise_models.py + 两个 router 初版 | 6 | 后端 2 人 |
| D2-D4 | 岗位匹配算法 job_matcher.py + 单元测试 | 算法通过 5 组模拟数据验证 | 3 | 后端 1 人 |
| D3-D5 | 企业端 3 个核心页（评价工作台+岗位匹配榜+仪表盘） | EEvaluate.vue + EJobMatch.vue + EDashboard.vue | 8 | 前端 2 人 |
| D4-D5 | 代码解析 code_analyzer.py + 上传端点 | 可上传 .py/.zip 并出代码质量分 | 3 | 后端 1 人 |
| **里程碑 1** | **硬约束通过 + 企业端核心可跑通** | 龙芯部署截图交组长审核 + 企业端登录可用 | — | 全员自查 |

### 6.2 第二周：功能拉满 + 三层报表（28 人日）
| 日期 | 任务 | 交付物 | 投入 |
|------|------|--------|------|
| D6-D7 | 分步实训：Task/Submission 表改造 + 步骤提交页 | 步骤解锁模式跑通 | 5 |
| D6-D8 | 企业端剩余 3 页（岗位列表抽屉 + 学生企业画像 + 学生列表） | EJobs.vue 抽屉 + EStudentProfile.vue + EStudents.vue | 9 | 前端 3 人 |
| D8-D9 | 三层报表：班级 + 校级报表生成器 | report_generator.py 扩展 2 个函数 + 对应端点 | 5 | 后端 1 人 |
| D7-D10 | 代码抄袭检测 + 相似度热力图 | `/statistics/similarity` 接口 + 前端展示 | 4 |
| D9-D10 | 三方评分对比组件全局复用 | TripartiteCompare.vue + 结果页嵌入 | 5 |
| **里程碑 2** | **全部 P0 功能可演示** | 全员走一遍：3 角色各关键流程，Bug 清单 < 20 条 | — |

### 6.3 第三周：体验 + 创新亮点打磨（24 人日）
| 日期 | 任务 | 交付物 | 投入 |
|------|------|--------|------|
| D11-D12 | AI 评价进度反馈（SSE + 异步任务） | 上传时 5 步动画真实推送 | 4 |
| D11-D13 | Landing Page 品牌化 + 新手引导 Tour | 首屏视频 + 8 张轮播 + 3 角色 Tour | 7 | 前端 2 人 |
| D12-D14 | AI 智能助教（SSE 流式） | /api/ai-assistant/chat 可追问本次提交 | 5 | 前后端各 1 |
| D14-D15 | 三层画像 Statistics 页重构 | 三 Tab 切换，每图可下钻 | 5 |
| D15 | 评分一致性分析 + 差异热力图 | /api/statistics/consistency 端点 + 前端 | 3 |
| **里程碑 3** | **创新功能全通 + UX 达标** | 创新功能走查清单逐项验证 | — |

### 6.4 第四周：文档 + 答辩准备（23 人日）
| 日期 | 任务 | 交付物 | 投入 | 责任角色 |
|------|------|--------|------|---------|
| D16-D18 | 五份标准文档（每份 40+ 页） | 需求分析/功能设计/产品说明/测试报告/部署文档 | 8 | 产品 1 + 测试 1 |
| D17-D18 | 整体回归测试 + Bug 清零 | 缺陷清单 0 | 4 | 全员 + 测试 |
| D18-D19 | 7 分钟演示视频录制 + 剪辑 | 含字幕 + 旁白 + 红框高亮 | 3 | 产品 + 前端 |
| D19-D20 | 答辩 PPT（20-25 页）+ 演讲词 | 每页备注讲稿，计时 3+7=10 分钟 | 4 | 组长 + 产品 |
| D20 | 模拟答辩 + 问答准备 30 题 | QA 清单打印分发 | 4 | 全员 |
| **里程碑 4** | **交付冻结 + 答辩就绪** | 所有材料 3 份备份（本机+U盘+云盘） | — | 组长确认 |

---

## 附录：分阶段实施路线（上下文可控版 · 12 个独立小阶段）

> **设计原则**：每个阶段改动 2-8 个文件、500-1500 行代码，**1-2 次对话内可完成并独立验证**。每个阶段结束后上下文可安全释放，下一阶段从零开始接上。
> 
> **编号规则**：阶段字母 = 所属大模块（A=企业端骨架 / B=企业端算法 / C=前端 / D=代码评测 / E=分步实训 / F=报表 / G=体验 / H=创新 / I=部署），数字 = 执行顺序。

---

### 🅰️ 阶段 A1 · 企业端数据库表（启动第一步 · 必做）
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `backend/app/models/enterprise_models.py` ← 新建：4 张表 ② `backend/app/models/tables.py` ← 修改 users.role 枚举 ③ `backend/app/models/class_models.py` ← 修改 Class 加 enterprise_id |
| **具体工作** | 按计划书 2.1.1 写 4 张表的 SQLAlchemy 模型；users.role 枚举加 `enterprise`；Class 表加 enterprise_id 外键 |
| **验收标准** | 在 MySQL 中 `CREATE TABLE` 全部成功无报错；`desc enterprises` 等命令能看到字段；外键约束正确 |
| **预计代码量** | ~200 行 Python |
| **预计对话轮数** | 1 轮 |

### 🅰️ 阶段 A2 · 企业端后端 API 骨架（注册+登录+岗位 CRUD）
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `backend/app/routers/enterprise_router.py` ← 新建 ② `backend/app/main.py` ← 修改：注册新 router ③ `backend/app/utils/auth.py` ← 修改：支持 enterprise 角色发 JWT |
| **具体工作** | 实现企业注册/登录、岗位 CRUD（6 个基础端点）、企业导师 profile；把 enterprise_router 挂到 main.py；auth.create_token 支持 role="enterprise" |
| **验收标准** | Postman/curl 能调通：企业注册 → 登录拿 token → 创建岗位 → 查岗位列表 |
| **预计代码量** | ~500 行 Python |
| **预计对话轮数** | 1-2 轮 |

### 🅱️ 阶段 B1 · 岗位匹配算法服务（独立纯逻辑 · 可单测）
| 项 | 内容 |
|----|------|
| **改动文件**（2 个） | ① `backend/app/services/job_matcher.py` ← 新建 ② `backend/app/routers/job_match_router.py` ← 新建（包一层端点） |
| **具体工作** | 实现 `calculate_job_match()` 5 步算法 + `get_student_weighted_avg()` 辅助函数 + 4 个 job_match HTTP 端点 |
| **验收标准** | 写 5 组模拟数据（学生 ABC/岗位 XYZ）直接调用函数验证：match_score 范围 0-100、亮点/gaps 非空、雷达数据维度对齐 |
| **预计代码量** | ~350 行 Python |
| **预计对话轮数** | 1 轮 |

### 🅰️ 阶段 A3 · 企业端评价 + 三方评分对比 API
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `enterprise_router.py` ← 扩展：评价端点 + 学生浏览端点 ② 新建 `backend/app/routers/enterprise_compare.py`（或合并） ③ `backend/app/models/__init__.py` ← 如需要导出 EnterpriseEvaluation 模型 |
| **具体工作** | 实现：企业提交评价 POST /evaluate、待评价列表 GET /pending-evaluations、GET /submissions/{id}/compare 三方对比数据结构、GET /classes & /students/{id} 学生画像 |
| **验收标准** | 用已有 AI/教师评价数据 + 新插入的企业评价 → 三方对比 API 返回 dimension_compare 含 has_warning 字段 |
| **预计代码量** | ~450 行 Python |
| **预计对话轮数** | 1-2 轮 |

### 🅳️ 阶段 D1 · 代码文件解析 + 代码质量分析
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `backend/app/services/file_parser.py` ← 修改：加 .py/.java/.cpp/.zip 解析 ② `backend/app/services/code_analyzer.py` ← 新建：静态指标 + 抄袭检测 ③ `backend/app/routers/upload_eval.py` ← 修改：代码上传分支 |
| **具体工作** | zipfile 解压多文件；正则/AST 提取 代码行数/注释率/函数数；同任务 Jaccard 相似度算抄袭分；POST /upload-eval/code 端点 |
| **验收标准** | 上传一个包含 3 个 .py 的 zip：能提取每个文件内容、出静态指标表、相似度检测能标记两份雷同代码为≥85% |
| **预计代码量** | ~400 行 Python |
| **预计对话轮数** | 1-2 轮 |

### 🅴️ 阶段 E1 · 分步实训（表改造 + 后端评价逻辑分支）
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `tables.py` ← 修改：Task.steps + Submission.step_evidences 两 JSON 字段 ② `ai_evaluator.py` ← 修改：按步骤核查 prompt ③ `upload_eval.py` 或新文件：分步提交端点 |
| **具体工作** | Task 和 Submission 加两个 JSON 列；evaluate() 函数新增 step 模式参数：先逐条步骤核查再出维度分；POST /upload-eval/step 端点 |
| **验收标准** | 建一个含 3 个步骤的任务 → 分步提交证据 → AI 返回每步 pass/fail + 理由 + 维度总分 |
| **预计代码量** | ~300 行 Python |
| **预计对话轮数** | 1 轮 |

### 🅵️ 阶段 F1 · 三层报表生成器（班级 + 校级）
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `backend/app/services/report_generator.py` ← 新增 2 个函数 ② `backend/app/routers/report.py` ← 新增 2 个端点 ③ `backend/requirements.txt` ← 加 matplotlib（PDF 出图用） |
| **具体工作** | generate_class_report()：全班成绩分布+维度热力图+Top/Bottom10+薄弱维度嵌入PDF；generate_school_report()：课程对比+教师排名+全校雷达图 |
| **验收标准** | 手动插 30 条模拟评价 → 调 class-report 端点下载 PDF，打开能看到 3 张以上图表 + 文字统计 |
| **预计代码量** | ~450 行 Python |
| **预计对话轮数** | 1-2 轮 |

---

### 🅲️ 阶段 C1 · 前端路由 + 角色适配（企业端页面骨架）
| 项 | 内容 |
|----|------|
| **改动文件**（4 个） | ① `frontend/src/router/index.ts` ← +6 条企业端路由 ② `frontend/src/layout/MainLayout.vue` ← 新增 enterprise 侧边栏 ③ `frontend/src/views/Login.vue` ← 三角色 Tab 登录 ④ `frontend/src/views/enterprise/` 目录 + 6 个空壳页（`<template><div>占位</div></template>`） |
| **具体工作** | router 加 6 条 /app/enterprise/* 路由；MainLayout 的侧边栏加第三个 `<template v-if="role==='enterprise'">`；Login 页用 el-tabs 切「教师/学生」「企业导师」；新建 6 个 vue 占位文件保证路由不报错 |
| **验收标准** | 浏览器手输 URL 能进 6 个企业页不报 404；侧边栏企业菜单在 localStorage 存 role=enterprise 后出现 |
| **预计代码量** | ~400 行 Vue/TS |
| **预计对话轮数** | 1 轮 |

### 🅲️ 阶段 C2 · 企业评价工作台 + 三方对比组件 ⭐（答辩明星页）
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `EEvaluate.vue` ← 左右 50/50 分栏布局 ② `TripartiteCompare.vue` ← 新建：三方对比卡+差异高亮 ③ `api/enterprise.ts`（如无公共 api 封装就直接 axios 调用） |
| **具体工作** | 左栏：筛选+待评价列表卡片；右栏三 Tab：学生提交内容 / AI+教师评分 / 企业打分；第三 Tab 含岗位适配度滑块、面试建议三卡片按钮、面试亮点输入框+AI助手按钮占位；TripartiteCompare 组件复用到结果页 |
| **验收标准** | 点左栏一条→右栏加载；维度分输入数字→总分自动算；差异≥15的行自动淡红底色；组件嵌入 Result.vue 无样式冲突 |
| **预计代码量** | ~900 行 Vue |
| **预计对话轮数** | 2 轮（第 1 轮左栏+布局，第 2 轮右栏+对比组件） |

### 🅲️ 阶段 C3 · 岗位匹配榜页 + 双雷达图组件
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `EJobMatch.vue` ← 左学生榜+右两张图 ② `JobMatchRadar.vue` ← 新建：岗位门槛vs学生双雷达叠加 ③ `EStudentProfile.vue` ← 四象限画像页 |
| **具体工作** | EJobMatch：顶部岗位条+筛选，左卡片列表（学生匹配卡带5行门槛/得分对照+亮点/差距），右上直方图（ECharts），右下雷达图（复用 JobMatchRadar）；EStudentProfile：2×2 Grid，能力雷达+TOP5岗位榜+趋势线+企业评价记录 |
| **验收标准** | 加载模拟数据→TOP20 学生卡片渲染；双雷达两条线（红实线/蓝虚线）清晰可辨；四象限四张 ECharts 图无报错 |
| **预计代码量** | ~1100 行 Vue |
| **预计对话轮数** | 2 轮（第 1 轮 EJobMatch，第 2 轮 EStudentProfile + 雷达组件抽取） |

### 🅲️ 阶段 C4 · 企业仪表盘 + 岗位列表抽屉
| 项 | 内容 |
|----|------|
| **改动文件**（3 个） | ① `EDashboard.vue` ← 4 张 stat-card + 热力图 + 高分推荐 ② `EJobs.vue` + `EJobDrawer.vue`（或单文件内嵌 el-drawer） ③ `EStudents.vue` ← 学生人才库列表 |
| **具体工作** | Dashboard：stat-card 用现有 global.css .stat-card 类；热力图用 ECharts heatmap（行=岗位列=班级值=均分匹配）；EJobs：表格+筛选项+右上「新建岗位」按钮→打开 720px 抽屉，5 区块表单（基本信息/描述/技能要求配置/绑定班级/底部操作）；EStudents：分页表格+点击行跳画像 |
| **验收标准** | 新建岗位抽屉滑出无卡顿；技能要求配置加/删维度总权重实时计算到 100%；提交创建→列表即时新增一行 |
| **预计代码量** | ~900 行 Vue |
| **预计对话轮数** | 2 轮（第 1 轮 Dashboard，第 2 轮岗位管理+学生列表） |

---

### 🅶️ 阶段 G1 · 体验优化小项打包
| 项 | 内容 |
|----|------|
| **改动文件**（5-6 个） | ① `EvaluationProgress.vue` ← 新建 5 步动画 ② `upload_eval.py` + 新建 `sse_router.py` ← SSE 推送进度 ③ `LandingPage.vue` ← 重写：首屏+价值卡+轮播 ④ `Upload.vue` 等 ← 接入进度组件 ⑤ 可选：`TourGuide.vue` + driver.js 集成 |
| **具体工作** | SSE：asyncio 队列 + FastAPI StreamingResponse 返回 5 个 event（parse/match/ai_score/save/done）；进度组件每步动画切图标颜色；LandingPage 首屏一张大图背景，三张价值主张卡片水平排列 |
| **验收标准** | 上传后真的等 3-5 秒收到 5 次 SSE 事件→进度条逐步推进；LandingPage 在 1920 屏无横向滚动条 |
| **预计代码量** | ~800 行 |
| **预计对话轮数** | 2 轮 |

### 🅷️ 阶段 H1 · 创新功能打包（AI 助教 + 三层画像 + 一致性分析）
| 项 | 内容 |
|----|------|
| **改动文件**（4-5 个） | ① `ai_assistant_router.py` ← 新建：SSE 流式聊天 ② `Result.vue` ← 底部新增 AI 聊天侧栏 ③ `Statistics.vue` ← 三 Tab 改造 + 下钻弹窗 ④ `/statistics` 后端加 consistency 端点 |
| **具体工作** | AI 助教：prompt 注入学生本次提交内容+评分+薄弱维度；三层画像：Statistics 加三个 el-tab-pane，班级画像直方图点击柱子→弹窗显示该分数段学生列表；一致性：差异热力图+自动建议文案 |
| **验收标准** | 打开某评价结果→在 AI 助教输入框问"我哪里错了？"→回答含该学生具体维度名和分数；点班级直方图画柱→弹学生列表 |
| **预计代码量** | ~900 行 |
| **预计对话轮数** | 2 轮 |

---

### 🧭 阶段依赖关系图（有向无环）
```
A1(DB表) → A2(基础API) → A3(评价API)
  ↓                        ↓
B1(匹配算法) → C3(匹配榜+画像)
               ↑
C1(路由骨架) → C2(评价台+对比) → C4(仪表盘+岗位)
  ↓
D1(代码评测), E1(分步实训), F1(报表)   三者互相独立可与 C 系列并行
  ↓                    ↓
G1(体验优化) 需 C2/D1 先完
  ↓
H1(创新功能) 依赖 C2 + F1 基础数据
```

### ⚡ 推荐并行推进顺序（5 人队最优）
| 对话轮次 | 前端组(2人) | 后端组(2人) | 算法/产品(1人) |
|---------|-----------|-----------|--------------|
| 第 1 次 | C1 路由骨架 | A1 DB 表 + A2 基础 API | B1 匹配算法 + 单测 |
| 第 2 次 | C2 企业评价台(上) | A3 企业评价 + 对比 API | D1 代码评测 |
| 第 3 次 | C2 企业评价台(下)+组件 | F1 三层报表 | E1 分步实训 |
| 第 4 次 | C3 匹配榜+画像 | 整体联调 bug 修 | G1 体验(SSE进度) |
| 第 5 次 | C4 仪表盘+岗位 | 文档骨架搭 | H1 AI 助教 + 一致性 |
| 第 6 次 | G1 Landing 品牌化 | 部署脚本+龙芯适配 | 演示数据 + 验收 Checklist |

### 🔁 每阶段"启动模板"（下次对话直接复制粘贴）
每次你要开始一个新阶段，对我说：
> "开始阶段 A2（企业端基础 API），按计划书要求，先做 xxx 文件，再做 yyy。"

我会自动：①读取该阶段涉及的所有旧文件最新内容 ② 按计划书规格写代码 ③ 每写完一个文件就验证 ④ 交付后给你验收命令。

---

## 七、答辩核心亮点话术（3 分钟自我介绍必讲）

### 开场（30s）
> "各位评委老师好，我们是 XX 队，带来的作品是**知训云——高校-企业协同的 AI 实训评价平台**。"

### 3 个差异化卖点（每点 40s，共 2min）
| # | 卖点 | 对应赛题原文 | 支撑证据 |
|---|------|-------------|---------|
| 1 | **真正实现高校-企业协同场景** | 第 16 行："高校-企业协同实训时，打通实训成果与岗位需求的衔接" | 现场演示：三角色登录 → 企业发布岗位技能 → 学生实训 → 三方评分对比 → 岗位匹配榜 |
| 2 | **不是黑盒 AI，是可校准评价系统** | 第 11 行："规避人工评价的主观偏差，提升评价客观性" | 现场演示：AI-教师评分差异热力图 → 指出某维度持续差 23 分 → 自动建议"优化提示词" |
| 3 | **代码+文档+截图全格式 + 分步核查** | 第 30-32 行："支持文档/截图多格式、逻辑漏洞识别、步骤完整性核查" | 现场演示：上传 Python 项目 zip → 出代码质量分 + 抄袭风险标注 → 步骤进度条 |

### 结尾（30s）
> "知训云已完成龙芯 LoongArch + 银河麒麟的完整适配部署（展示截图），可直接落地院校实训基地。以上是我们的作品，谢谢各位评委！"

---

## 八、风险与应对

| 风险 | 概率 | 影响 | 应对预案 |
|------|------|------|---------|
| 龙芯虚拟机申请不到/卡顿 | 中 | 致命（0 分） | ① 提前 3 天催促管理员 ② 准备 x86 麒麟虚拟机做 Plan B，注明"龙芯机调配中已兼容验证" ③ 截图+视频多份备份 |
| 决赛现场网络差 AI 调用失败 | 高 | 严重 | ① 所有演示场景提前跑一遍存 Mock 数据 ② 本地 Ollama 兜底 ③ PPT 内嵌预录 GIF 动图演示关键步骤 |
| 4 周做不完企业端全部 | 中 | 中等 | 砍 P1 功能保 P0：企业端留 3 页核心（评价工作台/岗位匹配榜/仪表盘），其余做成"规划中"灰色入口 |
| 演示视频超 7 分钟 | 低 | 扣分项 | 精确到秒的脚本 + 剪辑时每段多留 2s 余量，总时长控制在 6:45 以内 |
| 答辩问答准备不足 | 中 | 影响 20% 文档演示分 | 准备 30 道标准问答：架构/安全/性能/AI幻觉/竞品/部署/定价/隐私/未来规划全覆盖 |

---

## 九、代码改动范围总览（便于 git 管理）

```
backend/
├── app/
│   ├── models/
│   │   ├── tables.py              ← 修改：users.role 枚举扩展
│   │   ├── class_models.py        ← 修改：Class 加 enterprise_id
│   │   └── enterprise_models.py   ← 新增：4 张企业端表
│   ├── routers/
│   │   ├── enterprise_router.py   ← 新增：企业端 20+ 端点
│   │   ├── job_match_router.py    ← 新增：岗位匹配 4 端点
│   │   ├── ai_assistant_router.py ← 新增：AI 助教聊天
│   │   ├── upload_eval.py         ← 修改：新增代码/分步上传分支
│   │   └── report.py              ← 修改：新增班级/校级报表
│   ├── services/
│   │   ├── code_analyzer.py       ← 新增：代码静态分析+抄袭检测
│   │   ├── job_matcher.py         ← 新增：岗位匹配算法
│   │   ├── report_generator.py    ← 修改：2 个新报表函数
│   │   └── file_parser.py         ← 修改：支持代码/zip 解析
│   └── utils/
│       └── ai_evaluator.py        ← 修改：代码评价 prompt + 分步核查
├── requirements.txt               ← 新增：ollama, matplotlib, python-Levenshtein
└── deploy.sh start.sh stop.sh     ← 新增：龙芯一键部署

frontend/src/
├── router/index.ts                ← 修改：+6 条企业端路由
├── layout/MainLayout.vue          ← 修改：+enterprise 侧边栏
├── views/Login.vue                ← 修改：三角色 Tab 登录
├── views/
│   ├── enterprise/                ← 新增目录：6 个页面
│   │   ├── EDashboard.vue
│   │   ├── EJobs.vue
│   │   ├── EJobDetail.vue
│   │   ├── EEvaluate.vue          ⭐ 核心
│   │   ├── EStudents.vue
│   │   ├── EStudentProfile.vue
│   │   └── EJobMatch.vue          ⭐ 答辩明星页
│   ├── Result.vue                 ← 修改：三栏对比 + AI 助教
│   ├── Statistics.vue             ← 修改：三 Tab 三层画像
│   ├── LandingPage.vue            ← 重写：品牌化升级
│   └── Upload.vue / StudentTasks.vue ← 修改：分步提交模式
└── components/common/
    ├── TripartiteCompare.vue      ← 新增：三方评分对比
    ├── JobMatchRadar.vue          ← 新增：双雷达图组件
    ├── TourGuide.vue              ← 新增：新手引导
    └── EvaluationProgress.vue     ← 新增：AI 评价 5 步进度条
```

**预估新增代码总量**：后端 ~2500 行 + 前端 ~6000 行 = **~8500 行**（不含文档和脚本）

---

## 十、验收 Checklist（决赛前 1 天逐项打勾）

### 硬约束
- [ ] 龙芯 LoongArch + 麒麟部署截图 ≥ 15 张，均清晰可辨
- [ ] 2 分钟部署视频可用，U 盘+云盘备份
- [ ] deploy.sh 在麒麟机上实测一键部署成功
- [ ] 赛题要求 5 份文档均 ≥ 40 页，格式规范（页眉页脚+目录+图表编号）
- [ ] 7 分钟演示视频时长 6:30-6:50，格式 mp4

### 功能 P0
- [ ] 三角色（教师/学生/企业）均可正常登录
- [ ] 企业端：发布岗位技能→查看学生→企业评价→岗位匹配榜 全流程跑通
- [ ] 代码上传：.py + .zip 解析成功，出代码质量分
- [ ] 分步实训：步骤 1 通过才解锁步骤 2，每步有 AI 判定
- [ ] 三层报表：班级报表+校级报表可导出 PDF/Excel
- [ ] 三方评分对比：差异 ≥15 分高亮红色预警显示

### 体验创新
- [ ] AI 评价：5 步进度动画真实推送，SSE 无报错
- [ ] AI 助教：追问"我哪里错了"可结合本次提交精准回答
- [ ] 三层画像：班级画像可点击分数段下钻学生列表
- [ ] Landing Page：龙芯/麒麟 Logo 墙可见，视频首屏播放正常

### 答辩材料
- [ ] PPT 20-25 页，每页备注讲稿，3+7=10 分钟演练 3 遍
- [ ] 30 道问答题打印分发，全员熟悉
- [ ] 所有材料 3 份备份：本机 + U 盘 × 2 + 云盘
- [ ] 演示账号提前登录（教师/学生/企业各一个），Cookie 保存好免现场输密码
- [ ] Mock 数据包：提前导入演示数据（3 班级 × 2 任务 × 30 学生提交）

---

**文档版本**：v1.0 / 2026-07-28
**下次评审节点**：D5 末（里程碑 1 完成后）修订 v1.1
