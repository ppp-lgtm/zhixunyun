# 知训云 · 项目功能介绍

> 第十五届中国软件杯参赛作品 — 基于AI的软件实训教学智能评价系统

---

## 一、系统定位

知训云是一款面向高校软件实训场景的**三端协同智能评价平台**，打通「教师出题 → 学生实训 → AI评分 → 教师复评 → 企业终评 → 岗位匹配」全链路。以「诚实、严肃、可溯源」为设计理念，用编辑杂志式 UI 承载试卷批改的隐喻，让实训评价从机械劳动变成数据驱动的可视化操作台。

**三端角色**：

| 角色 | 核心职责 | 页面数 |
|------|----------|--------|
| 教师 | 出题、建班、配置评价维度、人工复核、数据统计 | 8 |
| 学生 | 分步提交实训成果、查看AI反馈、追踪成长、接收面试邀约 | 8 |
| 企业 | 发布岗位、查看成果物、企业终评、岗位匹配、发送面试邀约 | 7 |

---

## 二、教师端功能

### 2.1 首页仪表盘（Home.vue）

教师登录后的统一入口，一屏掌握教学全局：

- **4 项 KPI 卡片**：未评分提交数、班级数、学生数、本月提交量
- **待办提交队列**：最近需要复核的学生提交，点击直达评价详情
- **月度提交趋势图**：ECharts 折线图展示近 6 个月提交量变化
- **快捷入口**：创建任务、管理班级、查看统计

### 2.2 任务管理（TaskManage.vue）

教师创建和管理实训任务的核心工作台：

- **任务列表**：按状态（进行中/已截止/草稿）分类展示，左侧色条标识状态
- **创建任务**：填写标题、要求、截止日期、总分；选择目标班级（支持多班）
- **AI 辅助生成任务要求**：调用 DeepSeek 自动生成任务描述和评分维度建议
- **提交情况一览**：每项任务显示已提交/未提交人数，点击查看明细
- **评价维度配置**：可自定义维度名称和权重（如代码质量40% + 文档规范30% + 界面设计30%）

### 2.3 班级管理（ClassManage.vue）

- **班级 CRUD**：创建/编辑/删除班级，填写年级、专业、学期、课程名
- **邀请码加入**：每班自动生成 6 位邀请码，学生凭码加入
- **成员管理**：查看班级学生名单，支持移除学生
- **多班关联**：一个任务可关联多个班级

### 2.4 班级成绩（ClassScores.vue）

- **成绩排名表**：按班级查看学生总分排名，支持按维度排序
- **成绩分布**：柱状图展示分数段分布（90+ / 80-89 / 70-79 / 60-69 / 不及格）
- **导出报告**：一键导出 Excel/PDF 格式的班级成绩报告（含图表、斑马纹、条件格式）

### 2.5 评价维度配置（Criteria.vue）

- **维度模板**：预设常用维度模板（代码质量/文档规范/界面设计/功能完整性）
- **自定义维度**：教师可新增/编辑/删除维度，设置名称、权重、描述
- **权重校验**：所有维度权重之和必须等于 100%
- **任务级配置**：每个任务可独立配置评价维度，互不影响

### 2.6 数据统计（Statistics.vue）

教师专属的数据分析仪表盘，多维度可视化教学效果：

- **成绩分布图**：饼图 + 柱状图展示全班成绩分布
- **维度均分雷达图**：五边形雷达图展示各维度班级平均水平
- **班级对比**：多班横向对比总分和各维度均分
- **提交率趋势**：折线图展示按时提交率变化
- **AI 教学建议**：基于班级数据自动生成教学改进建议文本

### 2.7 人工复核评分（teacher_score.py 接口）

- 教师可查看 AI 初评结果，对每个维度进行人工调分
- 覆盖评分后系统自动记录教师评分时间戳
- 支持「确认通过」一键采纳 AI 评分

### 2.8 报告导出（report.py 接口）

- **Excel 报告**：表头合并、色板配色、斑马纹行、冻结窗格、条件格式（分数色阶）、内嵌柱状图和雷达图，分 Sheet 展示概览和明细
- **PDF 报告**：多页排版，封面 + 核心指标卡 + matplotlib 图表 + 学生排行表，自动注册系统中文字体确保中文不乱码

---

## 三、学生端功能

### 3.1 首页仪表盘（Home.vue）

学生登录后的个人学习中心：

- **4 项 KPI 卡片**：待办任务数、已完成任务数、平均分、班级排名
- **待办任务列表**：未提交/已截止的任务提醒
- **成长趋势**：个人历次评价分数折线图
- **AI 岗位匹配卡片**：展示 TOP1 匹配岗位及匹配度

### 3.2 分步提交实训成果（Upload.vue）

三步式提交流程，引导学生规范交付：

**步骤 ① 任务简报**
- 展示任务要求、评分标准、截止日期
- 让学生明确「要做什么、怎么评分」

**步骤 ② 交付物上传**
- 支持多种格式：PDF / DOCX / 图片（PNG/JPG） / 代码文件 / ZIP 压缩包
- 拖拽上传，实时显示上传进度
- 分步骤提交证据：每步可上传代码文件 + 截图 + 文字说明

**步骤 ③ AI 评价**
- 上传后自动触发 AI 评价，显示处理进度
- 评价完成后跳转结果页

### 3.3 评价结果详情（Result.vue）

三标签页切换，全方位展示评价结果：

**Tab 1 · 智能核查**
- 步骤完整性时间线：每步 passed / partial / failed 状态节点
- 问题清单：AI 发现的逻辑漏洞、代码缺陷、文档缺失项

**Tab 2 · 三方对比**
- AI 初评 / 教师复评 / 企业终评 三列同屏对比
- 每个维度分数 + 总分 + 评语差异高亮

**Tab 3 · 评价趋势**
- 该学生历次评价分数变化曲线

### 3.4 我的任务（StudentTasks.vue）

- **任务列表**：按状态分类（待提交/进行中/已截止/已评分）
- 点击任务进入分步提交流程
- 查看已评分任务的详细评价结果

### 3.5 我的班级（MyClasses.vue）

- **已加入班级**：展示班级信息、教师、同学数
- **邀请码加入**：输入 6 位邀请码加入新班级
- **班级排名**：查看自己在班级中的排名

### 3.6 我的成绩（MyScores.vue）

- **成绩记录表**：历次任务的总分和各维度得分
- **能力雷达图**：个人能力五维可视化
- **成长报告**：分数趋势 + 弱项分析 + 改进建议

### 3.7 面试邀约（InterviewInvitations.vue）

学生端接收企业面试邀请的中心：

- **4 枚状态印章**：待回复 / 已接受 / 已拒绝 / 总数，印章风格计数卡
- **5 Tab 筛选**：全部 / 待回复 / 已接受 / 已拒绝 / 已撤回，带计数徽章
- **邀约详情**：日期印章、岗位标题、面试时间/地点/企业导师信息、企业留言
- **一键回复**：pending 状态可弹窗确认参加或婉拒，附回复留言

### 3.8 个人资料（Profile.vue）

- 编辑真实姓名、学号、邮箱、手机
- 上传头像
- 修改密码

---

## 四、企业端功能

### 4.1 企业首页仪表盘（EnterpriseDashboard.vue）

- **4 项 KPI**：发布岗位数、匹配候选人数、已评价数、评价平均分
- **最近岗位列表**：岗位状态和匹配人数概览
- **待评价学生队列**：需要企业终评的学生成果物

### 4.2 岗位管理（EnterpriseJobs.vue）

企业发布和管理招聘岗位：

- **岗位 CRUD**：创建/编辑/删除/上下架岗位
- **技能要求配置**：JSON 格式存储技能要求列表，支持设置每项技能的门槛分数和是否必选（must）
- **AI 辅助生成**：调用 AI 根据岗位描述自动生成技能要求建议
- **匹配参数**：配置各维度权重和最低匹配分数线
- **岗位状态管理**：开放 / 暂停 / 关闭

### 4.3 企业评价工作台（EnterpriseEvaluations.vue）

企业导师对学生成果物进行终评的五步流程：

| 步骤 | 内容 |
|------|------|
| ① 学生作品 | 查看学生提交的代码文件、截图、说明文字 |
| ② 参考评分 | 查看 AI 评分和教师评分作为参考 |
| ③ 企业打分 | 4 维度独立打分（功能完整性/代码质量/文档规范性/界面设计）+ 岗位匹配分 + 优势亮点 + 改进建议 |
| ④ 岗位匹配 | 实时查看该生与岗位的匹配度 |
| ⑤ 提交评价 | 提交企业评价，可选择同时发送面试邀约 |

- 仅展示 `is_enterprise_project=1` 的任务提交，确保企业只看到分配给自己的成果物
- 支持面试建议标签：recommend（推荐面试）/ maybe（待定）/ not_recommend（不推荐）

### 4.4 三方对标对比（EnterpriseCompare.vue）

AI / 教师 / 企业三方评价同屏对比分析：

- **筛选器**：按班级 / 任务 / 学生 / 日期范围多维度筛选
- **对比表格**：行=学生，列=三方各维度分数 + 总分 + 差异度等级（高/中/一致）
- **详情弹窗**：点击某行展开，三卡片展示整体概览 + 维度对比明细 + 各方评分理由
- **差异高亮**：三方分数差异大的维度自动标红

### 4.5 岗位匹配推荐（EnterpriseMatching.vue）

企业端核心功能 — 候选人排行榜：

- **选择班级 + 岗位**：按班级和岗位组合查询候选人
- **TOP N 排行榜**：按匹配度降序排列，展示环形匹配度分数
- **维度对比**：学生能力 vs 岗位门槛的雷达图对比
- **亮点与差距分析**：达标维度标绿（highlights），未达标标红（gaps），分 minor（差距≤15）/ major（差距>15）
- **下载 PDF 报告**：一键生成多页 PDF（封面 + 核心指标概览 + 图表 + 候选人排行表 + 详细画像）

### 4.6 候选人画像（EnterpriseStudentProfile.vue）

单个学生的企业视角深度画像：

- **3 项顶部指标**：TOP1 岗位匹配分、加权历史均分、TOP5 可投岗位数
- **4 Tab 统一卡片**：

| Tab | 内容 |
|-----|------|
| 能力对标 | 雷达图：该生能力 vs 全班均值对比 |
| TOP5 匹配岗位 | 柱状图：岗位门槛分 vs 该生匹配分 |
| 成长记录 | 评价趋势线 + 企业评价时间线 |
| 面试邀约 | 该生的面试邀约历史记录 |

### 4.7 数据隔离

企业端所有数据查询均按 `enterprise_id` 过滤：

- 企业 A 只能看到自己发布的岗位、自己收到的成果物、自己提交的企业评价
- 企业 B 无法访问企业 A 的任何数据
- 通过 `require_enterprise` 依赖注入获取当前企业身份，所有路由自动鉴权

---

## 五、AI 智能评价引擎

### 5.1 评价维度

系统默认 4 大评价维度，教师可自定义：

| 维度 | 说明 | 数据来源 |
|------|------|----------|
| 功能完整性 | 步骤完成度、功能覆盖度 | 步骤提取器 + LLM 判断 |
| 代码质量 | LOC、注释率、圈复杂度、TODO数 | 代码静态分析器 + LLM 判断 |
| 文档规范性 | README 完整性、注释规范、格式 | LLM 文本分析 |
| 界面设计 | UI 截图 OCR 识别、设计规范遵循度 | 硅基流动视觉 API + LLM 判断 |

### 5.2 AI 评分流程

```
学生上传文件
    ↓
file_parser.py 解析文本（docx/pdf/图片OCR）
    ↓
code_analyzer.py 代码静态分析（LOC/注释率/复杂度/TODO）
    ↓
step_extractor.py 步骤提取与完整性校验（三层回退：PaddleOCR → 云OCR → 规则兜底）
    ↓
ai_evaluator.py 组装 Prompt（System Prompt + Rubric + Few-shot + 文本证据）
    ↓
DeepSeek API 返回结构化 JSON
    ↓
_parse_llm_json_safe() 安全解析（三重围栏去除/布尔修复/二次修复）
    ↓
维度分数 0-100 夹值 + 顺序一致化
    ↓
保存到 evaluations 表 → 返回前端
```

### 5.3 评分 Rubric 设计

5 级分级评分标准，确保评分一致性：

| 等级 | 分数段 | 标准 |
|------|--------|------|
| 优秀 | 90-100 | 全面达标，细节出色，可作范例 |
| 良好 | 80-89 | 达标，少量瑕疵，不影响整体 |
| 合格 | 70-79 | 基本达标，有可见缺陷 |
| 待改进 | 60-69 | 部分达标，明显缺陷 |
| 不合格 | 0-59 | 未达标，存在严重问题 |

### 5.4 Prompt 工程

- **System Prompt**：定义 AI 角色（软件实训评审专家）和输出格式约束
- **分级 Rubric**：每个维度附带 5 级评分标准和判定要点
- **Few-shot 示例**：提供 2-3 个典型评分案例，校准评分尺度
- **输出格式**：强制 JSON 结构，含 `dimension_scores` / `total_score` / `comment` / `step_completeness` / `logic_issues`

### 5.5 JSON 安全解析

LLM 输出可能包含格式异常（多余围栏、Python 风格布尔值、截断等），`_parse_llm_json_safe()` 函数实现三层容错：

1. 正则提取 JSON 字符串（去除 ```json 围栏）
2. 修复 Python 风格布尔值（`True` → `true`）
3. 解析失败时调用 LLM 二次修复（temperature=0，只修格式不改内容）

---

## 六、代码静态分析与抄袭检测

### 6.1 代码质量分析（code_analyzer.py）

支持 Python / Java / C++ 三种语言，输出指标：

| 指标 | 说明 |
|------|------|
| LOC | 代码行数（不含空行和注释） |
| 注释率 | 注释行 / 总行数（含 Python docstring 识别） |
| 函数数 | def / function / void 等定义计数 |
| 类数 | class / struct 定义计数 |
| 圈复杂度 | 估算值（if/for/while/switch 分支数 + 1） |
| TODO / FIXME | 待办标记计数 |

### 6.2 抄袭检测

三信号融合算法，改标识符也能命中：

1. **标识符 Jaccard 相似度**：变量名/函数名集合的交集占比
2. **结构 Token Jaccard 相似度**：将标识符替换为 `ID`、数字替换为 `NUM`，保留控制结构的 token 序列比对
3. **长度相似度加分**：代码行数接近度修正

- 全班两两比对，按相似度阈值分级：`≥0.95` high / `≥0.90` medium / `≥0.85` low
- 输出 TOP-N 最相似代码对列表

---

## 七、岗位匹配算法（job_matcher.py）

### 7.1 学生能力计算

`get_student_weighted_avg()` — 取学生最近 N 条评价记录，按优先级（teacher > ai）去重，指数衰减加权：

- 最新一条权重 1.0，次新 decay，再次 decay²…
- 输出各维度的加权平均分

### 7.2 匹配度计算（5 步算法）

`calculate_job_match()` 核心公式：

```
逐维度贡献度 = min(学生维度分 / 门槛分, 1.2) × 维度权重
匹配总分 = Σ贡献度 / (1.2 × Σ权重) × 100

特殊情况：
  must=True 的技能任一未过门槛 → 额外扣 10 分
```

输出结构：
- `match_score`：0-100 匹配总分
- `highlights`：达标维度（学生分超出门槛 5+ 分）
- `gaps`：未达标维度（minor 差距≤15 / major 差距>15）
- `radar`：雷达图数据（学生能力 vs 岗位门槛）

### 7.3 批量匹配

- `batch_match_class()`：全班学生 × 单岗位 → TOP N 候选人榜
- `batch_match_student_to_jobs()`：单学生 × 全部岗位 → TOP 5 推荐岗位

---

## 八、实训步骤提取引擎（step_extractor.py）

### 8.1 步骤解析

从任务要求文本中自动解析步骤，支持 6 种格式：

- `步骤1` / `第一步` / `Step 1` / `1.` / `第一阶段` / 中文数字（一、二、三...）

### 8.2 图片 OCR 三层回退

| 优先级 | 方案 | 说明 |
|--------|------|------|
| 1 | PaddleOCR 本地模型 | 懒加载，首次调用才初始化，离线可用 |
| 2 | 云 OCR API | 百度通用文字识别（配置占位） |
| 3 | 规则兜底 | 文件名编号 + 关键词匹配 |

### 8.3 步骤完整性判定

- **文件名编号命中**：从图片文件名提取步骤号（`步骤1.png` / `step_1.jpg` / `01_登录.png`）
- **关键词命中**：中文 2 字 bigram + 英文单词 + 同义词表（zip↔打包、login↔登录），命中率 ≥50% 算 passed
- **上传顺序兜底**：按上传顺序对应步骤序号

输出：`{total_steps, passed, missing, percent, source, steps[]}`

---

## 九、报告导出系统

### 9.1 Excel 报告

使用 openpyxl 生成美化 Excel：

- 表头合并居中 + 品牌色填充
- 斑马纹行（偶数行浅色底）
- 冻结首行首列
- 条件格式：分数色阶（红→黄→绿）
- 内嵌图表：柱状图（成绩分布）+ 雷达图（维度对比）
- 分 Sheet 展示：概览 / 明细 / 图表

### 9.2 PDF 报告

使用 fpdf2 + matplotlib 生成多页 PDF：

- **封面页**：标题 + 班级/岗位 + 生成时间 + 品牌色块
- **核心指标概览**：4 格指标卡水平对齐
- **图表页**：matplotlib 生成柱状图/雷达图，内嵌为图片
- **候选人排行表**：斑马纹 + 匹配度色阶
- **详细画像**：每位候选人的维度分数 + 亮点 + 差距
- 中文字体自动注册（扫描 `C:/Windows/Fonts/msyh.ttc`）
- 临时图片文件生成后自动清理

### 9.3 文件下载机制

- 前端通过 axios 请求二进制流（`responseType: 'blob'`）
- 解析 `Content-Disposition` 头获取文件名（支持 `filename*=UTF-8''` 中文编码）
- 使用 `Blob` + `URL.createObjectURL` 触发浏览器下载
- 下载接口支持 `token` 查询参数（兼容 `<a>` 标签直接下载）

---

## 十、认证与安全

### 10.1 角色强隔离登录

- 前端三角色（学生/教师/企业）各自独立表单，登录时带 `expected_role` 参数
- 后端校验账号角色与请求角色匹配，不匹配统一返回 401 + 「用户名或密码错误」
- 不暴露「此账号属于XX端」信息，防止枚举攻击

### 10.2 JWT 认证

- HS256 签名，Token 包含 `user_id` + `role` + `exp`
- 前端存储在 localStorage，每次请求通过 `Authorization: Bearer <token>` 头携带
- 后端通过 `auth_deps.py` 提供依赖注入：`require_login` / `require_teacher` / `require_enterprise`

### 10.3 数据隔离

| 端 | 隔离字段 | 实现方式 |
|----|----------|----------|
| 企业端 | `enterprise_id` | 所有查询 `.filter(Enterprise.id == me.enterprise_id)` |
| 教师端 | `teacher_id` | 班级/任务按创建者过滤 |
| 学生端 | `student_id` | 提交/成绩按本人过滤 |

### 10.4 密码安全

- SHA256 加盐哈希存储（`hash_password` / `verify_password`）
- 每个密码独立盐值，防彩虹表

---

## 十一、演示数据

项目内置完整演示数据，一键导入即可用于比赛演示：

```bash
cd backend
python ..\演示数据\_apply_demo_seed_ALL.py
```

### 演示账号（密码统一 123456）

| 角色 | 账号 | 说明 |
|------|------|------|
| 教师 | admin9001 | 管理 2 个班级（软件工程1班 / 前端实训班） |
| 企业 | hr9001 | 星云智能 HR，发布 1 个前端岗位 |
| 学生 | s9001 | 王浩宇 — 优秀样例（任务三 94 分） |
| 学生 | s9002 | 刘潇雨 — 良好样例（84 分） |
| 学生 | s9003 | 陈博扬 — 中等样例（64 分） |
| 学生 | s9004 | 赵敏诗 — 中等样例（80 分） |
| 学生 | s9005 | 孙佳怡 — 良好样例（87 分） |
| 学生 | s9006 | 周子祺 — 良好样例（76 分） |
| 学生 | s9007 | 吴锋 — 中等样例（72 分） |
| 学生 | s9008 | 蒋楠 — 待改进样例（36 分） |

### 演示数据内容

- 3 个实训任务（用户认证模块 / 博客系统 / 企业级仪表盘）
- 12 条学生提交（含分步证据 JSON）
- 8 份企业评价（含维度分数、亮点、改进建议、面试建议）
- 3 份面试邀约
- 1 个招聘岗位（前端开发工程师，含技能要求 JSON）

### 演示流程

详细演示步骤见 [演示数据/演示脚本.md](file:///G:/b1提交物/code/zhixunyun/演示数据/演示脚本.md)，包含教师端和企业端的完整操作路径。

---

## 十二、技术架构总览

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Vue 3 + Vite)                │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ 教师端 8页 │  │ 学生端 8页 │  │    企业端 7页      │  │
│  └─────┬────┘  └─────┬────┘  └────────┬──────────┘  │
│        └──────────────┴────────────────┘             │
│                    axios + JWT                       │
└────────────────────────┬────────────────────────────┘
                         │ HTTP / JSON
┌────────────────────────┴────────────────────────────┐
│              后端 (FastAPI + SQLAlchemy)              │
│  ┌──────────────────────────────────────────────┐   │
│  │  路由层 (16 个 router)                         │   │
│  │  auth / enterprise / job_match / upload_eval  │   │
│  │  task / class / report / statistics / ...     │   │
│  └──────────────────────┬───────────────────────┘   │
│  ┌──────────────────────┴───────────────────────┐   │
│  │  服务层                                       │   │
│  │  file_parser / code_analyzer / step_extractor │   │
│  │  job_matcher / report_generator               │   │
│  └──────────────────────┬───────────────────────┘   │
│  ┌──────────────────────┴───────────────────────┐   │
│  │  AI 引擎                                      │   │
│  │  DeepSeek (文本评分) + 硅基流动 (图片OCR)       │   │
│  └──────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────┘
                         │ SQLAlchemy ORM
┌────────────────────────┴────────────────────────────┐
│                 MySQL / SQLite                        │
│  14 张表：账号/教师/学生/班级/任务/提交/评价/          │
│  评价标准/企业/企业导师/岗位/企业评价/面试邀约/班成员  │
└──────────────────────────────────────────────────────┘
```
